<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:myapp="http://schemas.android.com/apk/res-auto"
    android:id="@+id/relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:theme="@style/DayTheme">

    <ListView
        android:id="@+id/accountNameListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        myapp:layout_constraintBottom_toBottomOf="parent"
        myapp:layout_constraintEnd_toEndOf="parent"
        myapp:layout_constraintStart_toStartOf="parent"
        myapp:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addAccountFAB"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="16dp"
        android:clickable="true"
        android:contentDescription="Add new account."
        android:focusable="true"
        myapp:layout_constraintBottom_toBottomOf="@+id/accountNameListView"
        myapp:layout_constraintEnd_toEndOf="@+id/accountNameListView"
        myapp:srcCompat="@android:drawable/ic_menu_add" />

</androidx.constraintlayout.widget.ConstraintLayout>
